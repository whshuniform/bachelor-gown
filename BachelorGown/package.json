{
  "start": "NODE_OPTIONS=--openssl-legacy-provider && cross-env UMI_ENV=dev umi dev",
  "name": "BachelorGown",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "export NODE_OPTIONS=--openssl-legacy-provider && nuxt",
    "start": "export NODE_OPTIONS=--openssl-legacy-provider && nuxt start",
    "build": "export NODE_OPTIONS=--openssl-legacy-provider && nuxt build",
    "build:gh-pages": "export NODE_OPTIONS=--openssl-legacy-provider && DEPLOY_ENV=GH_PAGES nuxt build",
    "generate:gh-pages": "export NODE_OPTIONS=--openssl-legacy-provider && DEPLOY_ENV=GH_PAGES nuxt generate"
  },
  "dependencies": {
    "@vue/shared": "^3.2.45",
    "core-js": "^3.25.3",
    "crypto-js": "^4.1.1",
    "detect-inapp": "^1.4.0",
    "jszip": "^3.10.1",
    "nuxt": "^2.15.8",
    "uglifyjs-webpack-plugin": "^2.2.0",
    "vue": "^2.7.10",
    "vue-gtag": "^1.16.1",
    "vue-loader": "^15.10.0",
    "vue-server-renderer": "^2.7.10",
    "vue-template-compiler": "^2.7.10",
    "vue-toastr": "^2.1.2"
  }
}
