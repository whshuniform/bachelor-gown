export const ArtButton = () => import('../../../../components/ArtButton.vue' /* webpackChunkName: "components/art-button" */).then(c => wrapFunctional(c.default || c))
export const Loading = () => import('../../../../components/Loading.vue' /* webpackChunkName: "components/loading" */).then(c => wrapFunctional(c.default || c))
export const PageHeader = () => import('../../../../components/PageHeader.vue' /* webpackChunkName: "components/page-header" */).then(c => wrapFunctional(c.default || c))
export const Photo = () => import('../../../../components/Photo.vue' /* webpackChunkName: "components/photo" */).then(c => wrapFunctional(c.default || c))
export const PhotoCollection = () => import('../../../../components/PhotoCollection.vue' /* webpackChunkName: "components/photo-collection" */).then(c => wrapFunctional(c.default || c))
export const Preview = () => import('../../../../components/Preview.vue' /* webpackChunkName: "components/preview" */).then(c => wrapFunctional(c.default || c))

// nuxt/nuxt.js#8607
function wrapFunctional(options) {
  if (!options || !options.functional) {
    return options
  }

  const propKeys = Array.isArray(options.props) ? options.props : Object.keys(options.props || {})

  return {
    render(h) {
      const attrs = {}
      const props = {}

      for (const key in this.$attrs) {
        if (propKeys.includes(key)) {
          props[key] = this.$attrs[key]
        } else {
          attrs[key] = this.$attrs[key]
        }
      }

      return h(options, {
        on: this.$listeners,
        attrs,
        props,
        scopedSlots: this.$scopedSlots,
      }, this.$slots.default)
    }
  }
}
